extends layout.pug
block content
  section.desk
    h1= error.message
    img.desk-items(src='/static/img/desk.svg')
    a.button-primary-small(href='/') Go Home
    .paper
      h2= error.status
      pre= error.stack
    .tear